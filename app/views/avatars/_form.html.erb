<%= form_for(@avatar) do |f| %>
  <% if @avatar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@avatar.errors.count, "error") %> prohibited this avatar from being saved:</h2>

      <ul>
      <% @avatar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :generated %><br>
    <%= f.check_box :generated %>
  </div>
  <div class="field">
    <%= f.label :image_location %><br>
    <%= f.text_field :image_location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
